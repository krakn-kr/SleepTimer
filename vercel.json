{
    "version": 2,
    "builds": [
      { "src": "package.json", "use": "@vercel/node" },  // for backend (if server entry is in scripts)
      { "src": "dist/**", "use": "@vercel/static" }
    ],
    "routes": [
      { "src": "/api/(.*)", "dest": "dist/index.js" }
    ]
  }
  